import React from 'react'
import {shallow} from 'enzyme'
import Album from './Album'

it('Title should be rendered in an span', () => {
    
})

it('Album cover should be set to an image', () => {
    
})

it('Album cover img tag should contain an `alt` attribute', () => {
    
})






